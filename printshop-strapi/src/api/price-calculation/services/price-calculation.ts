/**
 * Price Calculation service - TypeScript
 * Strapi 5 requires TypeScript files for services
 */
import { factories } from '@strapi/strapi';

export default factories.createCoreService('api::price-calculation.price-calculation');
