/**
 * Price Calculation controller - TypeScript
 * Strapi 5 requires TypeScript files for controllers
 */
import { factories } from '@strapi/strapi';

export default factories.createCoreController('api::price-calculation.price-calculation');
