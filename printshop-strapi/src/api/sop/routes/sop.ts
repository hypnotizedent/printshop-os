/**
 * SOP (Standard Operating Procedure) router - TypeScript
 * Strapi 5 requires TypeScript files for routes
 */
import { factories } from '@strapi/strapi';

export default factories.createCoreRouter('api::sop.sop');
